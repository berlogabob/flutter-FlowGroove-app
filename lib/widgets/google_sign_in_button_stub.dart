import 'package:flutter/widgets.dart';

/// Non-web stub for the GIS button; never rendered off web
/// (login screen shows its own [OutlinedButton] there).
Widget googleSignInButton() =>
    throw UnsupportedError('googleSignInButton is web-only');
